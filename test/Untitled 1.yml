info:
  name: Untitled 1
  type: http
  seq: 1

http:
  method: GET
  url: ?order=akash
  params:
    - name: order
      value: akash
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
